# Welcome to this Invoices
A small program for keeping track of personal invoices.


### Commands to run:
composer:
```php
composer install
```

npm:
```javascript
npm install && npx mix

// To run compiling:
npx mix
```

sql:
```sql
/* Add SQL queries here for setting up DB tables */
```

### Environment file:
- Create a file in the root folder named: .env
- Copy all variables in .env.example and paste in .env
- Edit copied variables to match your setup

[Link to DB Scheme](https://drawsql.app/teams/pks-team-1/diagrams/invoices)
